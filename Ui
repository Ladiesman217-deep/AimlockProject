local base = 'https://raw.githubusercontent.com/17kShotsss/UI-LIBRARY/main/' -- backup, not taking any credits

local Library = loadstring(game:HttpGet(base .. 'Library.lua'))()
local ThemeManager = loadstring(game:HttpGet(base .. 'addons/ThemeManager.lua'))()
local SaveManager = loadstring(game:HttpGet(base .. 'addons/SaveManager.lua'))()

local Window = Library:CreateWindow({
    Title = 'Raycodex',
    Center = true, 
    AutoShow = true,
})

local Tabs = {
    Main = Window:AddTab('Main'), 
    Misc = Window:AddTab('Misc'),
    ['UI Settings'] = Window:AddTab('UI Settings'),
}

local LeftSilentAim = Tabs.Main:AddLeftGroupbox('Silent Aim')
local Visual = Tabs.Main:AddLeftGroupbox('Visual')
local Checks = Tabs.Main:AddRightGroupbox('Checks')
local Rage = Tabs.Main:AddRightGroupbox('Rage')
local Shop = Tabs.Misc:AddLeftGroupbox('Shop')

LeftSilentAim:AddToggle('SilentAimToggle', {
    Text = 'Silent Aim',
    Default = false,
}):AddKeyPicker('LockKeyBind', {
    Default = '..', 
    SyncToggleState = false, 
    Mode = 'Toggle',
    Text = 'Nun',
    NoUI = false,
})

LeftSilentAim:AddToggle('FovToggle', {
    Text = 'Fov Visible',
    Default = false,
})

LeftSilentAim:AddSlider('FovSlider', {
    Text = 'Fov',
    Default = 100,
    Min = 0,
    Max = 500,
    Rounding = 0,
    Compact = false,
})

LeftSilentAim:AddDropdown('HitPart', {
    Values = { 'Head', 'HumanoidRootPart' },
    Default = 1,
    Multi = false,
    Text = 'Hit Part',
})

LeftSilentAim:AddDropdown('Method', {
    Values = { 'Raycast', 'Manipulation' },
    Default = 1,
    Multi = false,
    Text = 'Method',
})

LeftSilentAim:AddDropdown('MethodAim', {
    Values = { 'FreeForAll', 'Toggled' },
    Default = 1,
    Multi = false,
    Text = 'AimMethod',
})


Checks:AddToggle('KO', {
    Text = 'K.O',
    Default = false,
})

Checks:AddToggle('Grabbed', {
    Text = 'Grabbed',
    Default = false,
})

Visual:AddToggle('Tracer', {
    Text = 'Tracer',
    Default = true,
})

Visual:AddToggle('HitPart', {
    Text = 'HitPart',
    Default = false,
})

Rage:AddToggle('CamOn', {
    Text = 'Camera On Target',
    Default = false,
}):AddKeyPicker('CamKeybind', {
    Default = '..', 
    SyncToggleState = false, 
    Mode = 'Toggle',
    Text = 'Nun',
    NoUI = false,
})

Rage:AddToggle('Orbit', {
    Text = 'Orbit',
    Default = false,
}):AddKeyPicker('OrbitEnabled', {
    Default = '..', 
    SyncToggleState = false, 
    Mode = 'Toggle',
    Text = 'Nun',
    NoUI = false,
})

Rage:AddToggle('Stomp', {
    Text = 'Stomp On Down',
    Default = false,
})

Rage:AddSlider('OrbitSpeed', {
    Text = 'Orbit Speed',
    Default = 5,
    Min = 1,
    Max = 10,
    Rounding = 0,
    Compact = false,
})

Rage:AddSlider('OrbitX', {
    Text = 'Orbit X',
    Default = 5,
    Min = 1,
    Max = 10,
    Rounding = 0,
    Compact = false,
})

Rage:AddSlider('OrbitZ', {
    Text = 'Orbit Z',
    Default = 5,
    Min = 1,
    Max = 10,
    Rounding = 0,
    Compact = false,
})

Rage:AddSlider('OrbitY', {
    Text = 'Orbit Y',
    Default = 5,
    Min = 1,
    Max = 10,
    Rounding = 0,
    Compact = false,
})

Shop:AddToggle('Armor', {
    Text = 'Auto Armor',
    Default = false,
})


-- <- Rage -> --

Options.OrbitSpeed:OnChanged(function()
    getgenv().Rage.Orbit.Speed = Options.OrbitSpeed.Value
end)

Options.OrbitZ:OnChanged(function()
    getgenv().Rage.Orbit.Position.Z = Options.OrbitZ.Value
end)
Options.OrbitY:OnChanged(function()
    getgenv().Rage.Orbit.Position.Y = Options.OrbitY.Value
end)

Options.OrbitX:OnChanged(function()
    getgenv().Rage.Orbit.Position.X = Options.OrbitX.Value
end)

Options.CamKeybind:OnClick(function()
    Toggles.CamOn:SetValue(not Toggles.CamOn.Value)
    getgenv().Rage.Cameraon = Toggles.CamOn.Value
end)

Toggles.CamOn:OnChanged(function()
    getgenv().Rage.Cameraon = Toggles.CamOn.Value
end)

Toggles.Stomp:OnChanged(function()
    getgenv().Rage.Stomp = Toggles.Stomp.Value
end)

Options.OrbitEnabled:OnClick(function()
    Toggles.Orbit:SetValue(not Toggles.Orbit.Value)
    getgenv().Rage.Orbit.IsEnabled = Toggles.Orbit.Value
end)

-- <- Rage End -> --





-- <- Shop -> --

Toggles.Armor:OnChanged(function()
    getgenv().Shop.AutoArmor = Toggles.Armor.Value
end)



-- <- Shop End -> --


-- <- Silent Aim -> --

Options.LockKeyBind:OnClick(function()
    Toggles.SilentAimToggle:SetValue(not Toggles.SilentAimToggle.Value)
    getgenv().SilentAimConfig.Enabled = Toggles.SilentAimToggle.Value
end)


Toggles.SilentAimToggle:OnChanged(function()
    getgenv().SilentAimConfig.Enabled = Toggles.SilentAimToggle.Value
end)

Toggles.SilentAimToggle:OnChanged(function()
    getgenv().SilentAimConfig.Enabled = Toggles.SilentAimToggle.Value
end)

Toggles.FovToggle:OnChanged(function()
    getgenv().SilentAimConfig.FovEnabled = Toggles.FovToggle.Value
end)

Options.MethodAim:OnChanged(function()
    getgenv().SilentAimConfig.MethodAim = Options.MethodAim.Value
end)

Options.HitPart:OnChanged(function()
    getgenv().SilentAimConfig.HitPart = Options.HitPart.Value
end)

Options.FovSlider:OnChanged(function()
    getgenv().SilentAimConfig.Fov = Options.FovSlider.Value
end)

-- <- Silent Aim End -> --


-- <- Visuals -> --

Toggles.HitPart:OnChanged(function()
    getgenv().Visuals.HitPart = Toggles.HitPart.Value
end)

Toggles.Tracer:OnChanged(function()
    getgenv().Visuals.Tracer = Toggles.Tracer.Value
end)


-- <- Visuals End -> --


-- <- Checks -> --

Toggles.KO:OnChanged(function()
    getgenv().Checks.KO = Toggles.KO.Value
end)


-- <- Checks End -> --


Library.KeybindFrame.Visible = false; 

Library:OnUnload(function()
    print('Unloaded!')
    Library.Unloaded = true
end)

-- UI Settings
local MenuGroup = Tabs['UI Settings']:AddLeftGroupbox('Menu')

-- I set NoUI so it does not show up in the keybinds menu
MenuGroup:AddButton('Unload', function() Library:Unload() end)
MenuGroup:AddLabel('Menu bind'):AddKeyPicker('MenuKeybind', { Default = 'End', NoUI = true, Text = 'Menu keybind' }) 

Library.ToggleKeybind = Options.MenuKeybind -- Allows you to have a custom keybind for the menu

-- Addons:
-- SaveManager (Allows you to have a configuration system)
-- ThemeManager (Allows you to have a menu theme system)

-- Hand the library over to our managers
ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)

-- Ignore keys that are used by ThemeManager. 
-- (we dont want configs to save themes, do we?)
SaveManager:IgnoreThemeSettings() 

-- Adds our MenuKeybind to the ignore list 
-- (do you want each config to have a different menu key? probably not.)
SaveManager:SetIgnoreIndexes({ 'MenuKeybind' }) 

-- use case for doing it this way: 
-- a script hub could have themes in a global folder
-- and game configs in a separate folder per game
ThemeManager:SetFolder('MyScriptHub')
SaveManager:SetFolder('MyScriptHub/specific-game')

-- Builds our config menu on the right side of our tab
SaveManager:BuildConfigSection(Tabs['UI Settings']) 

-- Builds our theme menu (with plenty of built in themes) on the left side
-- NOTE: you can also call ThemeManager:ApplyToGroupbox to add it to a specific groupbox
ThemeManager:ApplyToTab(Tabs['UI Settings'])

-- You can use the SaveManager:LoadAutoloadConfig() to load a config 
-- which has been marked to be one that auto loads!
