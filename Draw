local Library = {}

function Library:MakeLiner()
    local line = Drawing.new('Line')
    line.Visible = false
    return line
end

function Library:MakeCircle()
    local circle = Drawing.new('Circle') 
    circle.Visible = false
    return circle
end

return Library
